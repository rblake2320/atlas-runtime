# Verified Capabilities

This file only lists what the public wrapper proves today.

## Verified now
- Installable Python package with `atlas` CLI
- Standalone workspace bootstrap
- Append-only event spine with deterministic replay verification
- Durable task pipeline with owner-aware delivery steps
- Truthful `doctor`, `verify`, `gap-meter`, and `interop-replay` commands
- Backend registry with one verified local backend (`atlas_local`)
- Tool catalog with real execution against workspace state
- Agent registry with four local owner-aware workers
- Deterministic multi-agent loop tick (`atlas loop once`)
- Local `chat` command routed through the active backend
- Local dashboard UI
- Read-only MCP-like HTTP server for status inspection
- Portable test suite (`17 passed`)

## Not claimed here
- Provider-level parity with Claude, ChatGPT, Gemini, or Ollama
- 24h+ autonomy
- non-zero learning deltas
- non-zero external value wins
- live multi-provider orchestration proof
